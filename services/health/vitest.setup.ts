/**
 * Test setup for the health service. No global setup is needed today — this
 * file exists to mirror the convention used by the other workspace packages
 * and gives a single place to add Lambda test scaffolding later.
 */
export {}
